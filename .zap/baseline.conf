# ZAP baseline scan rule overrides — one rule per line:
#   <ruleId>\tIGNORE|WARN|FAIL\t(comment)
# Populate after the first baseline run surfaces the noise (expected: cookie
# Secure-flag warnings from running plain HTTP in CI, CSP/X-Content-Type-Options
# on responses where the upstream proxy normally adds them). When this file
# covers the accepted noise, drop -I from the zap-baseline job so real
# regressions break the pipeline.
