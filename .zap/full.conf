# ZAP full active scan rule overrides — one rule per line:
#   <ruleId>\tIGNORE|WARN|FAIL\t(comment)
# Populate after the first full-scan run on main surfaces noise. The full scan
# stays allow_failure: true until the report is clean enough to gate on.
