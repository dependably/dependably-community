# ZAP full active-scan rule overrides — one rule per line, TAB-separated:
#   <ruleId>	IGNORE|WARN|FAIL	(comment)
# Triaged from the authenticated AJAX-spider active scan of the SPA. The active
# scan finds NO attack-class issues (no injection/XSS/etc.) — only the same
# header/informational noise as the passive baseline. With these accepted,
# zap-full runs without -I and gates: a new active finding (a real injection an
# agent introduces) reds the pipeline. Re-green by triaging into this file.

# Spectre cross-origin isolation headers (COEP/COOP/CORP) — accepted, same as
# .zap/baseline.conf and the authenticated API scans.
90004	IGNORE	(Cross-origin isolation headers - not required for this app)
# ZAP flags any 10-digit integer as a possible Unix timestamp - false positive.
10096	IGNORE	(Timestamp Disclosure - numeric false positive)
# Informational observation that a CORS header is present.
40040	IGNORE	(CORS Header - informational)
# Informational fingerprint, not a finding.
10109	IGNORE	(Modern Web Application - informational)
# Informational cache-header observation.
10049	IGNORE	(Storable and Cacheable Content - informational)
# Luhn/BIN heuristic matches a digit run inside the W3C trace id echoed on responses
# (traceparent / problem+json traceId); no payment or PII data exists on this surface.
10062	IGNORE	(PII Disclosure - Luhn FP on echoed trace-id digit runs, not real PII)
# Fires on a private IP/CIDR string inside the compiled frontend i18n bundle
# (settings hint copy, e.g. the TRUSTED_PROXIES example) — public static-asset
# documentation text, not a server-side disclosure.
2	IGNORE	(Private IP Disclosure - example IP in frontend hint copy, not a leak)
# Fires where dropping the session cookie does not change the response — the SPA
# shell, static assets, and rate-limited (429) responses — which is expected, not a
# session-handling defect. Authenticated data routes still 401 without the cookie.
90027	IGNORE	(Cookie Slack Detector - static/SPA-shell/429 responses; session not required there)
