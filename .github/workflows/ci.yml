name: CI

on:
  push:
    branches: [main]
    tags: ['v*.*.*']
  pull_request:
    branches: [main]

jobs:
  unit-and-compliance:
    name: Unit, Compliance & Security Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup .NET
        uses: actions/setup-dotnet@9a946fdbd5fb07b82b2f5a4466058b876ab72bb2 # v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore

      - name: Check for vulnerable packages
        run: dotnet list package --vulnerable --include-transitive 2>&1 | tee /tmp/vuln.txt && ! grep -q "has the following vulnerable packages" /tmp/vuln.txt

      - name: Build (with SecurityCodeScan Roslyn analyzer — fails on SQL injection patterns)
        run: dotnet build --no-restore -c Release

      - name: Check for string interpolation in Dapper calls (SQL injection prevention)
        run: |
          # Match actual Dapper method-call sites: ".Execute*(" / ".Execute*<" /
          # ".Query*(" / ".Query*<". Anchoring on the leading '.' and a trailing
          # '(' or '<' avoids false positives on substrings like "SearchQueryService".
          if grep -rnE '\.(Execute|Query)[A-Za-z]*\s*[(<]' src/ --include="*.cs" | grep -E '\$"|\$@"'; then
            echo "ERROR: String interpolation detected in a potential Dapper query call."
            exit 1
          fi

      # SchemaPostgres tests require a live Postgres (TEST_POSTGRES_CONNECTION) and throw loudly
      # without one — they run only in GitLab's dedicated schema-integrity job. Exclude them here
      # so the offline unit suite stays green, matching the test class's stated design intent.
      - name: Test (unit + compliance + security)
        run: dotnet test --no-build -c Release --verbosity normal --filter "Category!=Integration&Category!=SchemaPostgres" --collect:"XPlat Code Coverage"

      - name: Upload unit coverage
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: unit-coverage
          path: '**/TestResults/**/coverage.cobertura.xml'

  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest
    # Skip when there's no prior commit to diff against (initial push,
    # force-push of a new branch). The scheduled deep scan in
    # secret-scan-deep.yml covers full-history scanning in those cases.
    if: github.event_name == 'pull_request' || github.event.before != '0000000000000000000000000000000000000000'
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0
      - uses: trufflesecurity/trufflehog@37b77001d0174ebec2fcca2bd83ff83a6d45a3ab # v3.95.3
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha || github.event.before }}
          head: ${{ github.event.pull_request.head.sha || github.sha }}
          extra_args: --only-verified

  frontend-lint:
    name: Frontend Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install
        run: npm ci
        working-directory: web

      - name: Lint
        run: npm run lint
        working-directory: web

      - name: Lint CSS
        run: npm run lint:css
        working-directory: web

      - name: Type-check (svelte-check)
        run: npm run check
        working-directory: web

      - name: Chrome emoji guard
        run: |
          if grep -rPn '[\x{1F300}-\x{1FAFF}\x{2600}-\x{27BF}\x{2700}-\x{27BF}\x{2300}-\x{23FF}]' web/src --include='*.svelte'; then
            echo "Emoji codepoint detected in web/src. Use a sprite from web/public/icons.svg instead — DESIGN.md §11." >&2
            exit 1
          fi

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup .NET
        uses: actions/setup-dotnet@9a946fdbd5fb07b82b2f5a4466058b876ab72bb2 # v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore -c Release

      - name: Integration Tests
        run: dotnet test --no-build -c Release --verbosity normal --filter "Category=Integration" --collect:"XPlat Code Coverage"

      - name: Upload integration coverage
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: integration-coverage
          path: '**/TestResults/**/coverage.cobertura.xml'

  sbom:
    name: Generate SBOMs
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Setup .NET
        uses: actions/setup-dotnet@9a946fdbd5fb07b82b2f5a4466058b876ab72bb2 # v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore dotnet tools
        run: dotnet tool restore

      - name: Generate backend SBOM
        run: dotnet CycloneDX src/Dependably/Dependably.csproj -o . -fn sbom-backend.json -F json -spv 1.6 --exclude-dev

      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: web

      - name: Generate frontend SBOM (production deps only)
        run: npm run sbom:prod
        working-directory: web

      # Merge the backend (NuGet) and frontend (npm) CycloneDX docs into one
      # hierarchical SBOM for the released image. Both inputs are production-only
      # (backend: --exclude-dev, frontend: --omit dev) and lockfile-derived, so the
      # result reflects the deps actually shipped. cyclonedx-cli is pinned by digest
      # (linux/amd64 — the GitHub-hosted runner is amd64).
      - name: Merge SBOMs
        run: |
          docker run --rm -v "$PWD":/work -w /work \
            cyclonedx/cyclonedx-cli@sha256:9a858a15e7b0843606efc0ff19d5f7575011a5428d7f3d343b4f6cf09d8f0d4e \
            merge --hierarchical \
            --input-files sbom-backend.json web/sbom-frontend-prod.json \
            --output-file sbom.json --output-format json --output-version v1_6 \
            --name "Dependably Community" --version "${GITHUB_REF_NAME#v}"

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: sbom
          path: |
            sbom.json
            sbom-backend.json
            web/sbom-frontend-prod.json

  publish:
    name: Publish Docker Image
    runs-on: ubuntu-latest
    needs: [unit-and-compliance, integration, frontend-lint, sbom]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write # create the GitHub release and upload the SBOM asset
      packages: write # push image to ghcr.io
      id-token: write # OIDC token for keyless sigstore signing
      attestations: write # store the build-provenance attestation on the repo
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      - name: Log in to GHCR
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@06116385d9baf250c9f4dcb4858b16962ea869c3 # v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build and push
        id: push
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          push: true
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/dependably:latest
            ghcr.io/${{ github.repository_owner }}/dependably:${{ steps.version.outputs.version }}
          platforms: linux/amd64,linux/arm64

      # Signed SLSA build provenance over the pushed image, by digest (keyless
      # OIDC/sigstore — no stored key). push-to-registry attaches the attestation
      # alongside the image so consumers can verify it by digest. This raises the
      # released image from SLSA Build L0 to L2. Verify with:
      #   gh attestation verify oci://ghcr.io/<owner>/dependably:<tag> -R <owner>/dependably
      - name: Attest build provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0
        with:
          subject-name: ghcr.io/${{ github.repository_owner }}/dependably
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      # Pull in the combined, prod-only CycloneDX SBOM built by the `sbom` job in this run.
      - name: Download SBOM
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: sbom
          path: .

      # Signed SBOM attestation bound to the same image digest (keyless OIDC/sigstore —
      # no stored key), pushed to the registry alongside the SLSA L2 build provenance
      # above. The action auto-detects CycloneDX (predicate type https://cyclonedx.org/bom).
      # Verify with:
      #   gh attestation verify oci://ghcr.io/<owner>/dependably:<tag> -R <owner>/dependably \
      #     --predicate-type https://cyclonedx.org/bom
      - name: Attest SBOM
        uses: actions/attest-sbom@c604332985a26aa8cf1bdc465b92731239ec6b9e # v4.1.0
        with:
          subject-name: ghcr.io/${{ github.repository_owner }}/dependably
          subject-digest: ${{ steps.push.outputs.digest }}
          sbom-path: sbom.json
          push-to-registry: true

      # Second, file-subject attestation so the SBOM is verifiable as a standalone
      # release download (subject = sbom.json's own digest, not the image's). This is
      # what makes `gh attestation verify sbom.json -R <owner>/dependably` resolve. The
      # emitted sigstore bundle travels with the release alongside the SBOM itself.
      - name: Attest SBOM file (release asset)
        id: attest_sbom_file
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4.1.0
        with:
          subject-path: sbom.json

      # Publish the signed, prod-only CycloneDX SBOM as a GitHub release asset so it is
      # downloadable by anyone who can read the public release. Idempotent: a tag re-run
      # uploads onto the existing release instead of failing. Uses the preinstalled gh
      # CLI rather than a third-party action to keep the dependency surface minimal.
      - name: Create GitHub release with signed SBOM
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          cp "${{ steps.attest_sbom_file.outputs.bundle-path }}" sbom.json.sigstore.json
          if gh release view "${GITHUB_REF_NAME}" >/dev/null 2>&1; then
            gh release upload "${GITHUB_REF_NAME}" sbom.json sbom.json.sigstore.json --clobber
          else
            gh release create "${GITHUB_REF_NAME}" sbom.json sbom.json.sigstore.json \
              --title "${GITHUB_REF_NAME}" \
              --notes "Signed production-dependency CycloneDX 1.6 SBOM for this release. Verify: \`gh attestation verify sbom.json -R ${GITHUB_REPOSITORY}\`"
          fi
