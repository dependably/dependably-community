apiVersion: v1
kind: Service
metadata:
  name: {{ include "dependably-edge.fullname" . }}
  labels:
    {{- include "dependably-edge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "dependably-edge.selectorLabels" . | nindent 4 }}
